[workspace]
resolver = "3"
members = [
    "kernel",
    "crates/pe-parser", "crates/x86-decode", "crates/aarch64-decode",
]
exclude = [
    "tools/bootimage",
]

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
lto = true
